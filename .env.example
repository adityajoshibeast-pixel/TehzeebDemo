# Copy this file to .env.local for local testing (vercel dev picks it up automatically).
# In production, set the same variable in Vercel dashboard → Project → Settings → Environment Variables.
GROQ_API_KEY=your_groq_key_here
